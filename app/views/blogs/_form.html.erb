<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :blog_body %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :blog_image %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject, id: :blog_subject %>
  </div>

  <div class="field">
    <%= form.label :user_id %><br />
    <%= form.number_field :user_id, id: :blog_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
